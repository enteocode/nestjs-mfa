export enum EventType {
    ENABLED = 'mfa.enabled',
    DISABLED = 'mfa.disabled',
    FAILED = 'mfa.failed',
    RECOVERY_ENABLED = 'mfa.recovery.enabled',
    RECOVERY_DISABLED = 'mfa.recovery.disabled',
    RECOVERY_USED = 'mfa.recovery.used',
    RECOVERY_FAILED = 'mfa.recovery.failed'
}
